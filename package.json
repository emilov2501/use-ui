{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:useui": "nx run useui-ts:build",
    "dev:www": "nx dev www --skip-nx-cache",
    "dev:useui": "nx run useui-ts:dev --skip-nx-cache",
    "release": "lerna publish from-package --no-private",
    "version": "lerna version --no-private"
  },
  "devDependencies": {
    "lerna": "8.1.2"
  }
}
